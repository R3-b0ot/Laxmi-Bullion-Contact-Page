# Laxmi Bullion Contact Page

Static GitHub Pages-compatible contact page with assets organized under `assets/`:

- `assets/images/` for header and reference imagery
- `assets/logo/` for the split logo SVGs
- `assets/css/` for the page stylesheet
- `assets/js/` for device-aware download behavior
